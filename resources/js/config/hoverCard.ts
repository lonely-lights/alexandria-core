/**
 * Hover card timing configuration (in milliseconds).
 * Adjust these to fine-tune how hover cards feel.
 */
export const HOVER_CARD = {
    /** Delay before showing the hover card */
    showDelay: 750,
    /** Delay before closing the hover card after mouse leaves */
    closeDelay: 300,
};
