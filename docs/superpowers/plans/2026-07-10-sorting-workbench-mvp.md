# Sorting Workbench MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or executing-plans. Spec (BINDING, incl. the Round-2 approved MVP cut + deferred list): `docs/superpowers/specs/2026-07-10-sorting-workbench-design.md`.

**Goal:** the two-level Sorting Workbench, Level 1 leading — roster/toggles/routing-text/preview/review for routing; Run + Review (+ read-only Hone) for level-2 creation.

**Stack/repos:** core C:\Websites\alexandria\alexandria-core `feat/workbench` (Inertia page + sections, all UI); app C:\Websites\alexandria\alexandria-app `feat/level2-context` continues (routes/controllers/endpoints; L2 services already there). UTF-8 no BOM; no PowerShell file writes; NEVER migrate:fresh/refresh; laravel/ai fakes only in tests; lang keys for all strings (ai.php or a new workbench group — follow how pages/AI strings resolve today); Andrew's test style; pint; `npm run build`; trailers:
`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`
`Claude-Session: https://claude.ai/code/session_01Eq6ibpxnULaMqdYdrUqd3n`

**Study before each task:** core `pages/AI/AiDashboard.tsx` + `Sections/*` (aesthetic + tab conventions + how AI pages fetch), the fable-audit review-console DESIGN DNA (keyboard A/S/G verdicts, versus cards, tabs, bulk bar — reimplement natively, don't port HTML), app `AiCommandController` + `AiDashboardController`, `MinimalClassifierAgent` (what the L1 prompt actually renders — find the classifier the local:notes toolkit uses; routing text = blueprint description + notebook description), notables/notebook_notes placement mechanics (re-route = the same moves my apply-scripts did: clear+insert pivots, ai_notes.routed_*).

### Task 1: Shell + gate + L1 roster
App: route `/p/{project:slug}/ai/workbench` (`ai.workbench`, gate: config('alexandria.workbench.enabled') default `env('APP_ENV')==='local'` — config in CORE's alexandria.php with a publish note — plus can:update,project), `WorkbenchController@show` passing: roster (blueprints: id/slug/name/allow_ai_sorting/description/routed-count via notables; notebooks: id/slug/title/allow_ai_sort/is_catch_all/description/note-count via notebook_notes), pending/unsorted counts. PATCH endpoints to flip the two toggle fields (policy-checked). Core: `pages/AI/Workbench.tsx` shell — top-level LEVEL tabs (Routing | Creation), Routing tab = roster cards grid (native tokens; toggle switch, catch-all badge, counts, description preview) with optimistic toggle + Inertia partial reload. Pest: gate on/off, roster payload counts, toggle endpoints incl. authorization. Browser smoke: page renders roster, toggle flips + persists.

### Task 2: Routing text edit + L1 prompt preview
App: PATCH description endpoints (blueprint/notebook — reuse existing update paths if they exist, else minimal endpoints w/ validation) + GET `ai/workbench/l1-prompt` returning the classifier's rendered prompt (extract the render from the classifier path into a service method; NO AI call). Core: roster card expands → textarea editing the routing text (save/cancel, char count) + a "Preview classifier prompt" drawer/modal rendering the returned prompt (mono block, copy button). Pest for both endpoints; Vitest only if pure helpers emerge.

### Task 3: L1 routing review + re-route
App: GET routed notes by destination (paginated ~50, filters: destination id/kind, unreviewed-first if a flag exists — else created order) + POST re-route `{note_id, to: {kind, id}}` performing the established move (clear bp/entry pivots + notebook rows for the note per the apply-script semantics — verify against NoteController/existing services for a reusable move; record ai_notes.routed_* update). Core: Review section under Routing tab — destination TABS (console DNA), note cards (title + full text scroll), keys A(keep)/G(flag→ai_notes review_flag)/S(skip)/R(re-route picker), bulk keep-all per tab, running counts. Pest: reroute correctness incl. ai_notes; browser smoke: tab render + a re-route round-trip.

### Task 4: L2 Run + Review + read-only Hone
App: endpoints — pending-notes summary per blueprint, POST preview-prompt (Level2ContextBuilder render, no AI), POST run-batch (dispatch a queued job wrapping the drain's batch path; sync fallback acceptable v1 if queueing is heavy — note choice), reusing AiCommandController for review ops. Core: Creation tab — Run pane (blueprint picker + counts + batch size + [Preview]/[Run]), Hone strip (read-only: rendered prompt + source map listing blueprint desc/fields/edges+ai_instruction/index counts w/ links to their edit homes), Review pane: batch tabs, per-NOTE versus cards (note text vs proposed commands: entry name/fields/relationship chips), A/G keys + bulk + [Execute approved] → results badges (executed/failed/dedup annotations). Supersede CommandsTab: leave it but add a deprecation pointer comment (no dual maintenance). Pest: preview/run endpoints (fakes), execute flow already covered core-side. Browser smoke: preview renders for location; fake-run → commands appear → approve → execute path (fakes/sqlite).

### Verification
Full suites both repos + new browser smokes + `npm run build`. ACCEPTANCE (operator, with Andrew): he opens the workbench, sees the roster with his real toggles/counts, previews the location L2 prompt, runs the REAL location pilot (his gate — AI cost), reviews 52 notes' commands in the tabs, executes, sees locations + relationships in his tree.
